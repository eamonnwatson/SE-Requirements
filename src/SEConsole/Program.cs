using SELibrary;
using System;

namespace SEConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            var parser = new Parser();

            parser.GetData();
        }
    }
}
